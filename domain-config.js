// 🎯 小鸡AI营销系统 - 域名跳转配置
// 第11步：域名跳转逻辑

const DOMAIN_CONFIG = {
    // ============================================
    // PC端域名列表
    // ============================================
    pcDomains: [
        'www.example.com',      // 主域名
        'pc.example.com',       // PC子域名
        'desktop.example.com',  // 桌面版子域名
        // 添加你的PC域名
    ],
    
    // ============================================
    // 移动端域名列表
    // ============================================
    mobileDomains: [
        'm.example.com',        // 移动端子域名
        'mobile.example.com',   // 移动版子域名
        'h5.example.com',       // H5子域名
        // 添加你的移动端域名
    ],
    
    // ============================================
    // 开发环境域名（不做跳转）
    // ============================================
    devDomains: [
        'localhost',
        '127.0.0.1',
        '192.168.',             // 局域网IP前缀
        '10.0.',                // 局域网IP前缀
        'file://',              // 本地文件
    ],
    
    // ============================================
    // 跳转目标页面
    // ============================================
    targets: {
        pc: 'pc.html',          // PC端目标页面
        mobile: 'index.html'    // 移动端目标页面
    },
    
    // ============================================
    // 跳转配置
    // ============================================
    options: {
        // 是否启用域名跳转
        enabled: true,
        
        // 是否启用设备检测（作为备用方案）
        enableDeviceDetection: true,
        
        // 是否保留URL参数
        preserveQueryString: true,
        
        // 是否保留URL锚点
        preserveHash: true,
        
        // 是否在控制台输出调试信息
        debug: true,
        
        // 跳转延迟（毫秒），0表示立即跳转
        delay: 0
    }
};

// 导出配置
if (typeof module !== 'undefined' && module.exports) {
    module.exports = DOMAIN_CONFIG;
}
